export enum SymbolKey {
    /**
     * 全部配置数据
     */
    GlobalConfig = 'config',
}

export enum FunctConfig {
    epPropKey = '__epPropKey',
}

export enum DefaultConfig {
    /**
     * 组件前缀
     */
    alias = '',
}
